let HISTORY = [];
let MAXLEN = 32;

const history = {
	getLatest: () => HISTORY[0],
	push: (txt) => {
		HISTORY.unshift(txt);
		if (HISTORY.length > MAXLEN) {
			HISTORY = HISTORY.slice(MAXLEN);
		}
	},
	all: () => HISTORY,
	get: (i) => HISTORY[i],
}

module.exports = history;